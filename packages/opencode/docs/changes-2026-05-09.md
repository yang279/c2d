# 基础修改详细记录

**提交**: `c3b80dc0e` (2026-05-09)
**分支**: dev

---

## 一、功能变更概述

本次修改包含三大核心功能：

1. **build → octo_ai 重命名** — 将默认 agent 从 `build` 重命名为 `octo_ai`
2. **Session Category 分类系统** — 为 session 添加分类功能，按 agent 类型自动归类
3. **新增 4 个专业化 Agent** — octo_insight、octo_make、octo_design、octo_canva

---

## 二、详细文件变更

### 2.1 Agent 系统重构 (`src/agent/agent.ts`)

**变更内容**:

| 变更项 | 说明 |
|--------|------|
| 主 agent 重命名 | `build` → `octo_ai` |
| 新增 `skills` 字段 | `Info` Schema 添加 `skills: Schema.optional(Schema.Array(Schema.String))` |
| plan agent 添加 `hidden: true` | 防止 plan agent 显示在用户切换列表中 |
| backward compat shim | 配置中 `build` 键自动映射到 `octo_ai` |
| get() 别名 | `get("build")` 自动返回 `octo_ai` |
| ctx.worktree 空值保护 | `path.relative(ctx.worktree, ...)` 添加条件判断，修复 Electron sidecar 崩溃 |
| 新增 4 个 agent | octo_insight、octo_make、octo_design、octo_canva |

**新增 Agent 定义**:

```typescript
octo_insight: {
  name: "octo_insight",
  description: "Expert at analyzing user interview recordings...",
  prompt: PROMPT_OCTO_INSIGHT,
  mode: "primary",
  native: false,
  skills: ["interview-analysis"],
}

octo_make: {
  name: "octo_make",
  description: "Web design prototyping specialist...",
  prompt: PROMPT_OCTO_MAKE,
  mode: "primary",
  native: false,
  skills: ["html-prototype"],
}

octo_design: {
  name: "octo_design",
  description: "UI design specialist...",
  prompt: PROMPT_OCTO_DESIGN,
  mode: "primary",
  native: false,
  skills: ["design-basics"],
}

octo_canva: {
  name: "octo_canva",
  description: "Creative material generation specialist...",
  prompt: PROMPT_OCTO_CANVA,
  mode: "primary",
  native: false,
  skills: ["creative-assets"],
}
```

---

### 2.2 Skill 系统扩展 (`src/skill/index.ts`)

**变更内容**:

| 变更项 | 说明 |
|--------|------|
| 新增 `getMany` 方法 | 批量获取多个 skill 信息 |
| 内置 agent skills 发现 | 通过 `import.meta.dir` / `__dirname` 发现 `src/agent/skills/` 目录 |
| Flag 重命名 | `OPENCODE_*` → `OCTO_*` |
| Pattern 重命名 | `OPENCODE_SKILL_PATTERN` → `OCTO_SKILL_PATTERN` |
| InstanceState 初始化修复 | 添加 `.pipe(Effect.orDie)` 消除错误通道 |

**新增代码片段**:

```typescript
// Built-in agent skills bundled with the package
const metaDir = (typeof import.meta.dirname !== "undefined" ? import.meta.dirname : undefined) ?? import.meta.dir ?? (typeof __dirname !== "undefined" ? __dirname : undefined)
if (metaDir) {
  const builtinSkillsDir = path.join(metaDir, "..", "agent", "skills")
  if (yield* fsys.isDir(builtinSkillsDir)) {
    yield* scan(state, builtinSkillsDir, SKILL_PATTERN, { scope: "builtin" })
  }
}
```

---

### 2.3 Session Category 分类系统

**新增文件**:

| 文件 | 说明 |
|------|------|
| `src/session/session-category.ts` | 分类服务实现 |
| `src/session/session-category.sql.ts` | Drizzle ORM 表定义 |
| `migration/.../migration.sql` | 数据库迁移脚本 |

**分类映射**:

```typescript
AGENT_TO_CATEGORY = {
  octo_ai: "dev",
  build: "dev",      // backward compat
  octo_design: "design",
  octo_make: "prototype",
  octo_insight: "analysis",
  octo_canva: "creative",
  plan: "planning",
}
```

**集成点**:

- `src/session/session.ts` — 创建 session 时自动分类；get/listByProject/listGlobal/children 都通过 LEFT JOIN 获取 category
- `src/session/projectors-next.ts` — agent 切换时更新分类（AgentSwitched.Sync）
- `src/server/projectors.ts` — session.updated 事件投影时 LEFT JOIN SessionCategoryTable

**待完成**:

- `src/server/routes/instance/session.ts` — API 尚未添加 `category` 查询参数（按分类过滤功能未实现）
- `src/cli/cmd/stats.ts` — stats 命令未传递 category（Bug #9，记录在 bug.md）

---

### 2.4 新增 Agent Prompt 文件

| 文件 | Agent | 功能 |
|------|-------|------|
| `src/agent/prompt/octo_insight.txt` | octo_insight | 用户访谈分析专家，协调 MCP 服务处理音视频 |
| `src/agent/prompt/octo_make.txt` | octo_make | Web 设计原型专家，生成 HTML/Tailwind CSS 原型 |
| `src/agent/prompt/octo_design.txt` | octo_design | UI 设计专家，使用 Pixso MCP 生成 .pix 设计文件 |
| `src/agent/prompt/octo_canva.txt` | octo_canva | 创意素材生成专家，生成图片/视频等创意资产 |

---

### 2.5 新增内置 Skills 目录

| 文件 | Skill | 说明 |
|------|-------|------|
| `src/agent/skills/octo_insight/interview-analysis/SKILL.md` | interview-analysis | 用户访谈分析方法论 |
| `src/agent/skills/octo_make/html-prototype/SKILL.md` | html-prototype | HTML 原型生成指南 |
| `src/agent/skills/octo_design/design-basics/SKILL.md` | design-basics | UI 设计基础原则 |
| `src/agent/skills/octo_canva/creative-assets/SKILL.md` | creative-assets | 创意素材生成指南 |

---

### 2.6 配置系统更新 (`src/config/`)

**config/config.ts**:
- Flag 引用从 `OPENCODE_*` → `OCTO_*`
- 添加 `octo_ai` 命名键支持

**config/agent.ts**:
- Schema 添加 `skills` 字段支持
- 添加 `/octo/` agent 配置目录 pattern

---

### 2.7 Provider Schema 更新 (`src/provider/schema.ts`)

```typescript
// 新增 octo provider ID
octo: schema.make("octo"),
```

---

### 2.8 文本/Prompt 更新

| 文件 | 变更 |
|------|------|
| `src/session/prompt/default.txt` | `opencode` → `Octo AI`，品牌更新 |
| `src/session/prompt/build-switch.txt` | `from plan to build` → `from plan to implementation` |
| `src/tool/plan-exit.txt` | `switch to build agent` → `switch to octo_ai agent` |

---

### 2.9 TUI/CLI 更新

| 文件 | 变更 |
|------|------|
| `src/cli/cmd/tui/routes/session/index.tsx` | `set("build")` → `set("octo_ai")` |
| `src/tool/plan.ts` | 所有 "build agent" → "octo_ai agent" |
| `src/tool/registry.ts` | Flag 引用更新，`ProviderID.opencode` → `ProviderID.octo` |

---

### 2.10 测试文件更新

| 文件 | 变更说明 |
|------|----------|
| `test/agent/agent.test.ts` | ~30+ 处 `build` → `octo_ai`，新增 backward compat 测试 |
| `test/skill/skill.test.ts` | 预期计数 +4（内置 agent skills），新增空 skill 场景测试 |
| `test/session/system.test.ts` | mock 添加 `getMany` 方法 |
| `test/tool/skill.test.ts` | `OPENCODE_TEST_HOME` → `OCTO_TEST_HOME` |

---

### 2.11 其他工具/脚本更新

| 文件 | 变更 |
|------|------|
| `script/seed-e2e.ts` | 新增 E2E 测试数据种子脚本 |
| `script/httpapi-exercise.ts` | Flag 引用更新 |
| `bin/octo` | 新增 octo CLI 入口脚本 |

---

## 三、Flag 全局重命名

以下 Flag 从 `OPENCODE_*` 重命名为 `OCTO_*`（涉及 `packages/core/src/flag/flag.ts` 及全代码库引用）：

| 旧名称 | 新名称 |
|--------|--------|
| OPENCODE_DISABLE_EXTERNAL_SKILLS | OCTO_DISABLE_EXTERNAL_SKILLS |
| OPENCODE_DISABLE_CLAUDE_CODE_SKILLS | OCTO_DISABLE_CLAUDE_CODE_SKILLS |
| OPENCODE_EXPERIMENTAL_WORKSPACES | OCTO_EXPERIMENTAL_WORKSPACES |
| OPENCODE_EXPERIMENTAL_PLAN_MODE | OCTO_EXPERIMENTAL_PLAN_MODE |
| OPENCODE_EXPERIMENTAL_LSP_TOOL | OCTO_EXPERIMENTAL_LSP_TOOL |
| OPENCODE_EXPERIMENTAL_MARKDOWN | OCTO_EXPERIMENTAL_MARKDOWN |
| OPENCODE_ENABLE_QUESTION_TOOL | OCTO_ENABLE_QUESTION_TOOL |
| OPENCODE_ENABLE_EXA | OCTO_ENABLE_EXA |
| OPENCODE_CLIENT | OCTO_CLIENT |

---

## 四、数据库迁移

**migration.sql**:

```sql
CREATE TABLE `session_category` (
  `session_id` text PRIMARY KEY REFERENCES `session`(`id`) ON DELETE CASCADE,
  `category` text NOT NULL,
  `time_created` integer,
  `time_updated` integer
);
CREATE INDEX `session_category_category_idx` ON `session_category` (`category`);
```

---

## 五、向后兼容性保证

1. **配置文件兼容**: 用户配置中使用 `build` 键会自动映射到 `octo_ai`
2. **API 调用兼容**: `get("build")` 自动返回 `octo_ai` agent
3. **默认 agent 兼容**: `default_agent: "build"` 自动解析为 `octo_ai`

---

## 六、修复的 Bug

1. **Electron sidecar 崩溃**: `ctx.worktree` 为 undefined 时 `path.relative()` 抛出异常
2. **内置 skills 未被发现**: Electron utility process 中 `import.meta.dir` 为 undefined