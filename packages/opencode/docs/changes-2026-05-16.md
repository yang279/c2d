# 5月16日工作记录

**日期**: 2026-05-16
**分支**: dev

---

## 一、工作概述

1. 实现 Agent 内置 MCP 工具绑定，按 agent 限定 MCP 工具访问范围
2. 合入 UXAI Studio 大型更新（studio 服务重构、图片生成工具、前端 UI 大量改动）
3. 修复 opencode provider 在新机器上无模型的问题
4. Studio 页面样式和设置弹窗

---

## 二、提交记录

### Commit: `509c3816` — feat: 实现 Agent 内置 MCP 绑定，仅声明 mcp 字段的 agent 可访问内置 MCP 工具

**新增文件**: `packages/opencode/src/config/builtin-mcp.ts`

**改动**:

| 文件 | 改动 |
|------|------|
| `src/agent/agent.ts` | 为各 agent 添加 `mcp` 字段声明 |
| `src/config/agent.ts` | Schema 新增 `mcp` 字段定义 |
| `src/config/builtin-mcp.ts` | 新增内置 MCP 绑定逻辑 |
| `src/config/config.ts` | 配置解析中注入 MCP 绑定 |
| `src/mcp/index.ts` | `toolsForAgent()` 过滤按 agent mcp 字段限定 |
| `src/session/prompt.ts` | prompt 构建时调用 `mcp.toolsForAgent()` |
| `test/config/builtin-mcp.test.ts` | 新增内置 MCP 绑定单元测试 |
| `test/session/prompt.test.ts` | 测试更新 |
| `test/session/snapshot-tool-race.test.ts` | 测试更新 |

---

### Commit: `8a910142` — fix(provider): 确保预制 opencode provider 在新机器上始终可用

**问题**: `bun dev`（CLI 模式）不运行 `generate.ts`，导致 `models-snapshot.js` 不存在。opencode provider 有 0 个模型。

**改动**:

| 文件 | 改动 |
|------|------|
| `api.json` | 新增 Octo AI provider 模型数据源（4 个模型） |
| `script/generate.ts` | 生成逻辑调整 |
| `src/provider/models.ts` | `populate()` 添加 `api.json` 兜底加载 |
| `src/provider/provider.ts` | opencode custom loader 添加 `api.json` 兜底 |
| `test/provider/models.test.ts` | 测试更新 |

---

### Commit: `949fe6d4` — feat(studio): 合入 UXAI Studio 更新并修复 session 列表过滤

**大型合入**，包含 Insight 页面多项增强和品牌更新：

| 模块 | 关键改动 |
|------|----------|
| **Insight 页面** | 新增 prompt-template-selector、action-bar 增强、mindmap-renderer（markmap SVG）、octo-tokens.css 样式、输出卡片检测工具（`utils/detect.ts`） |
| **Studio 服务** | `studio-service.ts` 大幅重构（261→522 行），图片生成工具输入格式调整 |
| **工具系统** | `jimeng_image_generate`、`internel_image_generate` 输入参数调整，registry 更新 |
| **配置/Provider** | `config.ts`、`models.ts`、`provider.ts` 重构；服务器 UI 路由更新 |
| **品牌更新** | 升级脚本、系统提示（anthropic/gpt/gemini 等）、share 页面更新 |
| **文档** | CLAUDE.md 大更新、desktop README 更新 |
| **i18n** | insight description 更新 |
| **测试** | 新增 `detect.test.ts`、`image-generate-inputs.test.ts`、`registry.test.ts` |

---

### Commit: `5fb1e583` — studio 添加设置，左边栏国际化

| 文件 | 改动 |
|------|------|
| `octoapp/pages/studio/index.tsx` | Studio 设置入口 |
| `octoapp/i18n/en.ts` | 新增 Studio 相关英文键 |
| `octoapp/i18n/zh.ts` | 新增 Studio 相关中文键 |

### Commit: `169e80d9` — fix session list

| 文件 | 改动 |
|------|------|
| `octoapp/pages/_shell/sidebar.tsx` | 修复 session 列表过滤逻辑（80→129 行重构） |

### Commit: `238c9c046` — hide usage btm

| 文件 | 改动 |
|------|------|
| `octoapp/style/index.css` | 隐藏 usage 底栏 |
| `octoapp/style/loading.css` | 新增加载动画样式 |
| `octoapp/public/OctoAI.svg` | 新增 OctoAI Logo SVG |

### Commit: `32e272290` — 修改studio样式

| 文件 | 改动 |
|------|------|
| `pages/studio/studio.css` | Studio 页面样式大幅扩展（+159 行） |
| `style/sidebar.css` | 侧边栏样式微调 |
| 新增 `public/studio/` 下 15+ 个 SVG 图标资源 |

### Commit: `8ead8d963` — add studio 设置弹窗

| 文件 | 改动 |
|------|------|
| `pages/studio/index.tsx` | 添加 Studio 设置弹窗组件 |

### Commit: `83a7fc4ff` — 修改样式

| 文件 | 改动 |
|------|------|
| `pages/studio/studio.css` | Studio 样式微调 |
| `style/sidebar.css` | 侧边栏样式微调 |

---

## 三、其他提交

| Hash | 说明 |
|------|------|
| `19562e041` | fix route — octo.tsx 路由修复 |
| `d08915ea` | docs: dev-plan 文档 octo_canva → octo_studio 命名更新 |
| `42adffc67` | Merge branch 'dev_ghy' |
| `279f82d36` | Merge pull request #8 |
