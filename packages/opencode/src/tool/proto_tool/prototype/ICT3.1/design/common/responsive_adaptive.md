# 响应式与适配

## 页面尺寸
页面默认采用1920*1080的宽度。

## 断点
- Mobile：小于 768px。本 skill 不承诺移动端完整适配，仅要求关键内容和主操作不遮挡、不可用时有清晰兜底。
- Tablet：768px 到 1024px。本 skill 不承诺平板端完整适配，仅要求核心流程可读、可操作、不出现严重布局破坏。
- Desktop：大于 1024px。桌面端是本 skill 的主要适配范围。
- 默认设计画布为 1920 x 1080；桌面端以 1920px 为基础设计宽度，默认 `1rem = 16px`。